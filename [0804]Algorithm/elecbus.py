tc= int(input())
for t in range(1,tc+1):
    K,N,M = map(int,input().split())
    chargerstop = list(map(int, input().split()))
    chargerstop_list = [0] * (N+1)

    for i in range():  
        chargerstop_list[chargerstop[i]] += 1
    